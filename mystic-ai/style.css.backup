/* ========== 基础设置 ========== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --primary: #8b5cf6;
    --primary-light: #a78bfa;
    --accent: #ec4899;
    --accent-light: #f472b6;
    --gold: #fbbf24;
    --eth: #627eea;
    --bg-dark: #030014;
    --bg-card: rgba(255, 255, 255, 0.03);
    --bg-card-hover: rgba(255, 255, 255, 0.06);
    --border: rgba(255, 255, 255, 0.08);
    --text: #f8fafc;
    --text-secondary: #94a3b8;
    --font-main: 'Space Grotesk', -apple-system, BlinkMacSystemFont, sans-serif;
}

body {
    font-family: var(--font-main);
    background: var(--bg-dark);
    color: var(--text);
    min-height: 100vh;
    overflow-x: hidden;
    line-height: 1.6;
}

/* ========== 动态背景光效 ========== */
.ambient-light {
    position: fixed;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle at 50% 50%, 
        rgba(139, 92, 246, 0.08) 0%,
        rgba(236, 72, 153, 0.05) 25%,
        transparent 50%);
    animation: ambientPulse 15s ease-in-out infinite;
    pointer-events: none;
    z-index: 0;
}

@keyframes ambientPulse {
    0%, 100% { transform: scale(1) rotate(0deg); opacity: 1; }
    50% { transform: scale(1.1) rotate(180deg); opacity: 0.8; }
}

/* ========== 粒子效果 ========== */
.particles {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 1;
    overflow: hidden;
}

.particle {
    position: absolute;
    width: 2px;
    height: 2px;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border-radius: 50%;
    opacity: 0;
    animation: particleFloat 25s infinite ease-in-out;
}

@keyframes particleFloat {
    0% {
        transform: translateY(100vh) translateX(0) scale(0);
        opacity: 0;
    }
    10% {
        opacity: 0.6;
    }
    90% {
        opacity: 0.6;
    }
    100% {
        transform: translateY(-100vh) translateX(100px) scale(1);
        opacity: 0;
    }
}

/* ========== 星星背景 ========== */
.stars {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 0;
}

.star {
    position: absolute;
    width: 2px;
    height: 2px;
    background: white;
    border-radius: 50%;
    animation: twinkle 3s ease-in-out infinite;
}

@keyframes twinkle {
    0%, 100% { opacity: 0.3; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.2); }
}

/* ========== 主容器 ========== */
.container {
    position: relative;
    z-index: 10;
    max-width: 560px;
    margin: 0 auto;
    padding: 60px 20px 80px;
}

/* ========== 头部 ========== */
.header {
    text-align: center;
    margin-bottom: 48px;
    animation: fadeInDown 0.8s ease-out;
}

@keyframes fadeInDown {
    from {
        opacity: 0;
        transform: translateY(-30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.logo-container {
    position: relative;
    width: 80px;
    height: 80px;
    margin: 0 auto 20px;
}

.logo-glow {
    position: absolute;
    inset: -10px;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border-radius: 50%;
    filter: blur(20px);
    opacity: 0.4;
    animation: glowPulse 3s ease-in-out infinite;
}

@keyframes glowPulse {
    0%, 100% { opacity: 0.4; transform: scale(1); }
    50% { opacity: 0.6; transform: scale(1.1); }
}

.logo-icon {
    position: relative;
    font-size: 2.5rem;
    background: linear-gradient(135deg, var(--primary-light), var(--accent-light));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: rotate 12s linear infinite;
}

@keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

.logo-text {
    font-size: 2rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    background: linear-gradient(135deg, var(--text), var(--primary-light));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 12px;
}

.tagline {
    font-size: 0.9rem;
    color: var(--text-secondary);
    letter-spacing: 0.1em;
    text-transform: uppercase;
}

/* ========== 玻璃卡片 ========== */
.glass-card {
    background: var(--bg-card);
    backdrop-filter: blur(40px);
    -webkit-backdrop-filter: blur(40px);
    border: 1px solid var(--border);
    border-radius: 24px;
    padding: 36px 32px;
    position: relative;
    overflow: hidden;
    animation: fadeInUp 0.6s ease-out;
}

@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.card-shine {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.05), transparent);
    animation: shine 8s ease-in-out infinite;
}

@keyframes shine {
    0%, 100% { left: -100%; }
    50% { left: 100%; }
}

/* ========== 语言切换按钮 ========== */
.language-switch {
    position: absolute;
    top: 16px;
    right: 16px;
    display: flex;
    gap: 8px;
    z-index: 100;
}

.lang-btn {
    padding: 8px 16px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(139, 92, 246, 0.3);
    border-radius: 8px;
    color: var(--text-secondary);
    font-size: 0.85rem;
    cursor: pointer;
    transition: all 0.3s ease;
}

.lang-btn:hover {
    background: rgba(139, 92, 246, 0.1);
    border-color: var(--primary);
}

.lang-btn.active {
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border-color: transparent;
    color: white;
}

/* ========== 表单标题 ========== */
.section-title {
    font-size: 1rem;
    font-weight: 500;
    text-align: center;
    margin-bottom: 32px;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.15em;
}

/* ========== 表单组 ========== */
.form-group {
    margin-bottom: 28px;
}

.form-group display: block;
    font-size: > label {
    0.8rem;
    font-weight: 500;
    color: var(--text-secondary);
    margin-bottom: 12px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

/* ========== 输入框 ========== */
.input-wrapper {
    position: relative;
}

.input-wrapper input {
    width: 100%;
    padding: 16px 20px;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--border);
    border-radius: 14px;
    color: var(--text);
    font-size: 1rem;
    font-family: inherit;
    transition: all 0.3s ease;
}

.input-wrapper input::placeholder {
    color: rgba(255, 255, 255, 0.25);
}

.input-wrapper input:focus {
    outline: none;
    border-color: var(--primary);
    background: var(--bg-card-hover);
    box-shadow: 0 0 0 4px rgba(139, 92, 246, 0.15);
}

.input-glow {
    position: absolute;
    inset: -2px;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border-radius: 16px;
    opacity: 0;
    filter: blur(8px);
    transition: opacity 0.3s;
    z-index: -1;
}

.input-wrapper input:focus + .input-glow {
    opacity: 0.5;
}

/* ========== 性别选择 ========== */
.gender-selector {
    display: flex;
    gap: 12px;
}

.gender-btn {
    flex: 1;
}

.gender-btn input {
    display: none;
}

.gender-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 6px;
    padding: 16px 12px;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--border);
    border-radius: 14px;
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 0.85rem;
    white-space: nowrap;
}

.gender-icon {
    font-size: 1.2rem;
    opacity: 0.6;
    transition: opacity 0.3s;
}

.gender-btn input:checked + .gender-content {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.2), rgba(236, 72, 153, 0.2));
    border-color: var(--primary);
    box-shadow: 0 0 20px rgba(139, 92, 246, 0.2);
}

.gender-btn input:checked + .gender-content .gender-icon {
    opacity: 1;
}

.gender-content:hover {
    background: var(--bg-card-hover);
    transform: translateY(-2px);
}

/* ========== 日期选择 ========== */
.date-selector {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 10px;
}

.select-wrapper {
    position: relative;
}

.select-wrapper select {
    width: 100%;
    padding: 16px 12px;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--border);
    border-radius: 14px;
    color: var(--text);
    font-size: 0.9rem;
    font-family: inherit;
    cursor: pointer;
    appearance: none;
    transition: all 0.3s ease;
}

.select-wrapper select:focus {
    outline: none;
    border-color: var(--primary);
    background: var(--bg-card-hover);
}

.select-wrapper select option {
    background: #1a1a2e;
    color: var(--text);
}

.select-glow {
    position: absolute;
    inset: -2px;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border-radius: 16px;
    opacity: 0;
    filter: blur(8px);
    transition: opacity 0.3s;
    z-index: -1;
}

.select-wrapper select:focus ~ .select-glow {
    opacity: 0.5;
}

.select-wrapper.full-width {
    grid-column: span 3;
}

.zodiac-display {
    font-size: 0.85rem;
    color: var(--text-secondary);
    margin-top: 12px;
    text-align: center;
}

#zodiacDisplay {
    color: var(--gold);
    font-weight: 600;
}

/* ========== 提交按钮 ========== */
.submit-btn {
    width: 100%;
    padding: 4px;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border: none;
    border-radius: 16px;
    cursor: pointer;
    position: relative;
    overflow: hidden;
    transition: all 0.4s ease;
    margin-top: 8px;
}

.submit-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 40px rgba(139, 92, 246, 0.4);
}

.submit-btn .btn-bg {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.2), transparent);
    opacity: 0;
    transition: opacity 0.4s;
}

.submit-btn:hover .btn-bg {
    opacity: 1;
}

.submit-btn .btn-text {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    padding: 18px 32px;
    background: linear-gradient(135deg, #1a1a2e, #0f0f1a);
    border-radius: 14px;
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text);
    position: relative;
    z-index: 1;
    transition: all 0.4s ease;
}

.submit-btn:hover .btn-text {
    background: transparent;
    color: white;
}

.submit-btn .btn-icon {
    display: inline-block;
    transition: transform 0.4s ease;
}

.submit-btn:hover .btn-icon {
    transform: rotate(180deg) scale(1.2);
}

/* ========== 加载动画 ========== */
.loading-container {
    text-align: center;
    padding: 80px 20px;
}

.tarot-animation {
    display: flex;
    justify-content: center;
    gap: 16px;
    margin-bottom: 40px;
}

.tarot-card-wrapper {
    perspective: 400px;
}

.tarot-card {
    width: 70px;
    height: 115px;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border-radius: 10px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
}

.tarot-card.flipping {
    animation: tarotFlip 2s ease-in-out infinite;
}

.tarot-card-wrapper.delay-1 .tarot-card {
    animation-delay: 0.15s;
}

.tarot-card-wrapper.delay-2 .tarot-card {
    animation-delay: 0.3s;
}

@keyframes tarotFlip {
    0%, 100% {
        transform: rotateY(0deg) translateY(0);
    }
    50% {
        transform: rotateY(180deg) translateY(-15px);
    }
}

.loading-text {
    font-size: 1.1rem;
    color: var(--text-secondary);
    margin-bottom: 24px;
    font-style: italic;
}

.loading-text-container {
    height: 30px;
    overflow: hidden;
    margin-bottom: 24px;
}

.loading-text-rotating {
    animation: rotateLoadingText 8s ease-in-out infinite;
}

@keyframes rotateLoadingText {
    0%, 20% { transform: translateY(0); opacity: 1; }
    25%, 45% { transform: translateY(-30px); opacity: 1; }
    50%, 70% { transform: translateY(-60px); opacity: 1; }
    75%, 95% { transform: translateY(-90px); opacity: 1; }
    100% { transform: translateY(0); opacity: 1; }
}

.loading-dots {
    display: flex;
    justify-content: center;
    gap: 10px;
}

.loading-dots span {
    width: 10px;
    height: 10px;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border-radius: 50%;
    animation: bounce 1.4s ease-in-out infinite both;
}

.loading-dots span:nth-child(1) { animation-delay: -0.32s; }
.loading-dots span:nth-child(2) { animation-delay: -0.16s; }

@keyframes bounce {
    0%, 80%, 100% { transform: scale(0.6); opacity: 0.4; }
    40% { transform: scale(1); opacity: 1; }
}

/* ========== 塔罗牌展示 ========== */
.tarot-reveal {
    text-align: center;
    margin-bottom: 36px;
    perspective: 600px;
}

.tarot-card-container {
    width: 160px;
    height: 250px;
    margin: 0 auto;
    position: relative;
    transform-style: preserve-3d;
    cursor: pointer;
    transition: transform 0.6s ease;
}

.tarot-card-container.revealed {
    transform: rotateY(180deg);
}

.tarot-card-face {
    position: absolute;
    inset: 0;
    backface-visibility: hidden;
    border-radius: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.tarot-card-face.front {
    background: linear-gradient(135deg, #1a1a2e, #2d1f4e);
    border: 2px solid var(--border);
}

.card-pattern {
    position: absolute;
    inset: 8px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 14px;
}

.card-symbol {
    font-size: 3.5rem;
    color: var(--gold);
    animation: rotate 8s linear infinite;
}

.tarot-card-face.back {
    background: linear-gradient(135deg, #1e1b2e, #3d1f4e);
    border: 2px solid var(--gold);
    transform: rotateY(180deg);
    flex-direction: column;
    padding: 20px;
}

.tarot-symbol-display {
    font-size: 3rem;
    margin-bottom: 12px;
}

.tarot-name {
    font-size: 0.9rem;
    color: var(--gold);
    letter-spacing: 0.15em;
    text-transform: uppercase;
}

/* ========== 结果网格 ========== */
.results-grid {
    display: flex;
    flex-direction: column;
    gap: 20px;
    margin-bottom: 36px;
}

.result-card {
    background: var(--bg-card);
    backdrop-filter: blur(40px);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 24px;
    transition: all 0.4s ease;
}

.result-card:hover {
    border-color: rgba(139, 92, 246, 0.3);
    transform: translateY(-3px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
}

.result-card.wide {
    /* 全宽样式在响应式中处理 */
}

.card-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 16px;
    padding-bottom: 12px;
    border-bottom: 1px solid var(--border);
}

.card-icon {
    font-size: 1.2rem;
}

.card-header h3 {
    font-size: 0.85rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-secondary);
}

.card-content {
    font-size: 0.9rem;
    line-height: 1.8;
    color: rgba(255, 255, 255, 0.85);
}

.card-content p {
    margin-bottom: 10px;
}

.card-content p:last-child {
    margin-bottom: 0;
}

/* ========== 重新测试按钮 ========== */
.restart-btn {
    width: 100%;
    padding: 16px 32px;
    background: transparent;
    border: 1px solid var(--border);
    border-radius: 14px;
    color: var(--text-secondary);
    font-size: 0.95rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    margin-bottom: 40px;
}

.restart-btn:hover {
    border-color: var(--primary);
    color: var(--text);
    background: rgba(139, 92, 246, 0.1);
}

.restart-icon {
    font-size: 1.1rem;
    transition: transform 0.4s ease;
}

.restart-btn:hover .restart-icon {
    transform: rotate(180deg);
}

/* ========== 返回按钮 ========== */
.back-btn {
    width: 100%;
    padding: 16px 32px;
    background: rgba(139, 92, 246, 0.1);
    border: 1px solid var(--primary);
    border-radius: 14px;
    color: var(--primary);
    font-size: 0.95rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    margin-bottom: 20px;
}

.back-btn:hover {
    background: var(--primary);
    color: white;
}

.back-icon {
    font-size: 1.1rem;
    transition: transform 0.3s ease;
}

.back-btn:hover .back-icon {
    transform: translateX(-4px);
}

/* ========== 捐赠卡片 ========== */
.footer {
    margin-top: 20px;
}

.donate-card {
    background: linear-gradient(135deg, rgba(98, 126, 234, 0.1), rgba(139, 92, 246, 0.1));
    backdrop-filter: blur(40px);
    border: 1px solid rgba(98, 126, 234, 0.3);
    border-radius: 24px;
    padding: 32px 28px;
    text-align: center;
    position: relative;
    overflow: hidden;
}

.donate-card::before {
    content: '';
    position: absolute;
    inset: -1px;
    background: linear-gradient(135deg, var(--eth), var(--primary), var(--accent));
    border-radius: 25px;
    z-index: -1;
    opacity: 0.5;
}

.donate-header {
    margin-bottom: 24px;
}

.donate-icon-wrapper {
    width: 56px;
    height: 56px;
    margin: 0 auto 16px;
    background: linear-gradient(135deg, var(--eth), var(--primary));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 8px 32px rgba(98, 126, 234, 0.3);
    animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
    0%, 100% { transform: scale(1); box-shadow: 0 8px 32px rgba(98, 126, 234, 0.3); }
    50% { transform: scale(1.05); box-shadow: 0 12px 40px rgba(98, 126, 234, 0.5); }
}

.donate-icon {
    font-size: 1.5rem;
    color: white;
}

.donate-header h3 {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 8px;
}

.donate-subtitle {
    font-size: 0.85rem;
    color: var(--text-secondary);
}

.wallet-display {
    background: rgba(0, 0, 0, 0.3);
    border-radius: 16px;
    padding: 20px;
    margin-bottom: 16px;
}

.wallet-label {
    font-size: 0.75rem;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 12px;
}

.wallet-address-wrapper {
    display: flex;
    align-items: center;
    gap: 12px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 12px;
    padding: 14px 16px;
}

.wallet-address {
    flex: 1;
    font-family: 'Space Grotesk', monospace;
    font-size: 0.85rem;
    color: var(--eth);
    letter-spacing: 0.05em;
    word-break: break-all;
    text-align: center;
}

.copy-btn {
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    background: rgba(98, 126, 234, 0.2);
    border: 1px solid rgba(98, 126, 234, 0.3);
    border-radius: 10px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
}

.copy-btn:hover {
    background: var(--eth);
    border-color: var(--eth);
    transform: scale(1.05);
}

.copy-icon {
    font-size: 1.1rem;
}

.donate-note {
    font-size: 0.85rem;
    color: var(--text-secondary);
    font-style: italic;
}

/* ========== 响应式设计 ========== */
@media (max-width: 480px) {
    .container {
        padding: 48px 16px 60px;
    }
    
    .glass-card {
        padding: 28px 24px;
    }
    
    .date-selector {
        gap: 8px;
    }
    
    .select-wrapper select {
        padding: 14px 8px;
        font-size: 0.85rem;
    }
    
    .tarot-card-container {
        width: 140px;
        height: 220px;
    }
    
    .wallet-address {
        font-size: 0.75rem;
    }
}

/* ========== 结果卡片入场动画 ========== */
.result-card {
    opacity: 0;
    transform: translateY(20px);
}

.result-card.animate-in {
    animation: resultCardIn 0.6s ease forwards;
}

@keyframes resultCardIn {
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* ========== 卡片悬浮效果 ========== */
.glass-card:hover {
    border-color: rgba(139, 92, 246, 0.2);
}

/* ========== 滚动条美化 ========== */
::-webkit-scrollbar {
    width: 6px;
}

::-webkit-scrollbar-track {
    background: var(--bg-dark);
}

::-webkit-scrollbar-thumb {
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border-radius: 3px;
}

/* ========== 邮件订阅区域 ========== */
.newsletter-section {
    margin-top: 20px;
    margin-bottom: 20px;
}

.newsletter-card {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.08), rgba(98, 126, 234, 0.08));
    backdrop-filter: blur(40px);
    border: 1px solid rgba(139, 92, 246, 0.2);
    border-radius: 20px;
    padding: 28px 24px;
    text-align: center;
    position: relative;
    overflow: hidden;
}

.newsletter-icon-wrapper {
    width: 48px;
    height: 48px;
    margin: 0 auto 14px;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 6px 24px rgba(139, 92, 246, 0.25);
}

.newsletter-icon {
    font-size: 1.3rem;
    color: white;
}

.newsletter-card h3 {
    font-size: 1rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 6px;
}

.newsletter-subtitle {
    font-size: 0.8rem;
    color: var(--text-secondary);
    margin-bottom: 20px;
}

.newsletter-form {
    display: flex;
    gap: 10px;
    max-width: 400px;
    margin: 0 auto;
}

.newsletter-form .input-wrapper {
    flex: 1;
}

.newsletter-form input {
    width: 100%;
    padding: 14px 16px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(139, 92, 246, 0.2);
    border-radius: 12px;
    color: var(--text);
    font-size: 0.9rem;
    outline: none;
    transition: all 0.3s ease;
}

.newsletter-form input:focus {
    border-color: var(--primary);
    background: rgba(255, 255, 255, 0.08);
}

.newsletter-form input::placeholder {
    color: var(--text-secondary);
}

.newsletter-btn {
    padding: 14px 24px;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border: none;
    border-radius: 12px;
    color: white;
    font-size: 0.9rem;
    font-weight: 500;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 6px;
    transition: all 0.3s ease;
    white-space: nowrap;
}

.newsletter-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(139, 92, 246, 0.35);
}

.newsletter-note {
    font-size: 0.75rem;
    color: var(--text-secondary);
    margin-top: 14px;
    font-style: italic;
}

/* 成功状态 */
.newsletter-card.success {
    border-color: rgba(34, 197, 94, 0.3);
}

.newsletter-card.success .newsletter-icon-wrapper {
    background: linear-gradient(135deg, #22c55e, #16a34a);
}

.newsletter-form {
    display: none;
}

.newsletter-card.success .newsletter-form {
    display: flex;
}

.success-message {
    display: none;
    color: #22c55e;
    font-size: 0.95rem;
    font-weight: 500;
}

.newsletter-card.success .success-message {
    display: block;
}

@media (max-width: 480px) {
    .newsletter-form {
        flex-direction: column;
    }
    
    .newsletter-btn {
        justify-content: center;
    }
}

/* ========== 历史记录区域 ========== */
.history-section {
    margin-top: 20px;
    margin-bottom: 20px;
}

.history-toggle-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    width: 100%;
    padding: 14px 20px;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.1), rgba(98, 126, 234, 0.1));
    border: 1px solid rgba(139, 92, 246, 0.2);
    border-radius: 14px;
    color: var(--text);
    font-size: 0.95rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s ease;
}

.history-toggle-btn:hover {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.15), rgba(98, 126, 234, 0.15));
    border-color: rgba(139, 92, 246, 0.3);
    transform: translateY(-2px);
}

.history-icon {
    font-size: 1.2rem;
}

.history-count {
    background: linear-gradient(135deg, var(--primary), var(--accent));
    padding: 2px 10px;
    border-radius: 20px;
    font-size: 0.8rem;
    color: white;
}

.history-panel {
    margin-top: 12px;
    background: rgba(0, 0, 0, 0.3);
    border: 1px solid rgba(139, 92, 246, 0.2);
    border-radius: 16px;
    padding: 20px;
    max-height: 400px;
    overflow-y: auto;
}

.history-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
    padding-bottom: 12px;
    border-bottom: 1px solid rgba(139, 92, 246, 0.2);
}

.history-header h3 {
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--text);
}

.clear-history-btn {
    padding: 6px 14px;
    background: rgba(239, 68, 68, 0.2);
    border: 1px solid rgba(239, 68, 68, 0.3);
    border-radius: 8px;
    color: #ef4444;
    font-size: 0.8rem;
    cursor: pointer;
    transition: all 0.3s ease;
}

.clear-history-btn:hover {
    background: rgba(239, 68, 68, 0.3);
    transform: scale(1.05);
}

.history-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.history-item {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(139, 92, 246, 0.1);
    border-radius: 12px;
    padding: 14px 16px;
    cursor: pointer;
    transition: all 0.3s ease;
}

.history-item:hover {
    background: rgba(139, 92, 246, 0.1);
    border-color: rgba(139, 92, 246, 0.3);
    transform: translateX(4px);
}

.history-item-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
}

.history-item-name {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--text);
}

.history-item-date {
    font-size: 0.75rem;
    color: var(--text-secondary);
}

.history-item-zodiac {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 4px 10px;
    background: rgba(139, 92, 246, 0.2);
    border-radius: 20px;
    font-size: 0.75rem;
    color: var(--primary);
}

.history-item-preview {
    font-size: 0.85rem;
    color: var(--text-secondary);
    line-height: 1.5;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.empty-history {
    text-align: center;
    color: var(--text-secondary);
    font-size: 0.9rem;
    padding: 20px;
}
